[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
[deploy]
startCommand = "/bin/sh -c \"exec gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120\""
healthcheckPath = "/api/db_info"
healthcheckTimeout = 300
